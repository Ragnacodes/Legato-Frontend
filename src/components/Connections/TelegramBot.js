import React from 'react';
import { Dialog } from '@material-ui/core';

const TelegramBot = ({telegramDialog, setTelegramDialog}) => {
    return (
        <Dialog>
            
        </Dialog>
    )
}

export default TelegramBot;